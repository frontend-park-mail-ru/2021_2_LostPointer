<div class='track-list'>
    <div class='suggested-tracks__header'>{{{title}}}</div>
    <div class='suggested-tracks-container'>
        {{#each tracks}}
            {{{this}}}
        {{/each}}
    </div>
</div>
